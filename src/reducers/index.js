const initialState = {
  regionLevel: "",
  region: "",
  scenarioCollection: "",
  scenarios: [],
  period: "",
  indicators: [],
  graphType: ""
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
